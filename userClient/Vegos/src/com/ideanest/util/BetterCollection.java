package com.ideanest.util;

/**
 * Marker interface to indicate that a collection class has had its
 * basic bugs fixed, and respects <code>Lockable</code> collection
 * arguments (though it may not be <code>Lockable</code> itself).
 */
public interface BetterCollection {
}
